<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment - Activity Registration System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f5f5f5;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            max-width: 600px;
            width: 100%;
        }

        header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 2rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            text-align: center;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
        }

        .subtitle {
            font-size: 1.2rem;
            opacity: 0.9;
        }

        .payment-container {
            background: white;
            border-radius: 10px;
            padding: 2rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .payment-details {
            background: #f8f9fa;
            padding: 1.5rem;
            border-radius: 5px;
            margin-bottom: 1.5rem;
        }

        .payment-details h2 {
            color: #667eea;
            margin-bottom: 1rem;
        }

        .payment-info {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .payment-info p {
            margin-bottom: 0.5rem;
        }

        .payment-methods {
            margin-bottom: 1.5rem;
        }

        .payment-methods h3 {
            margin-bottom: 1rem;
            color: #333;
        }

        .payment-option {
            display: flex;
            align-items: center;
            margin-bottom: 1rem;
            padding: 1rem;
            border: 1px solid #ddd;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .payment-option:hover {
            border-color: #667eea;
            background: #f8f9ff;
        }

        .payment-option.selected {
            border-color: #667eea;
            background: #f0f4ff;
        }

        .payment-option input {
            margin-right: 1rem;
        }

        .payment-icon {
            font-size: 1.5rem;
            margin-right: 1rem;
        }

        .btn {
            display: inline-block;
            padding: 0.8rem 1.5rem;
            background: #667eea;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            border: none;
            cursor: pointer;
            font-size: 1rem;
            transition: background 0.3s ease;
            margin-right: 0.5rem;
            margin-bottom: 0.5rem;
        }

        .btn:hover {
            background: #5a6fd8;
        }

        .btn-success {
            background: #28a745;
        }

        .btn-success:hover {
            background: #218838;
        }

        .btn-secondary {
            background: #6c757d;
        }

        .btn-secondary:hover {
            background: #5a6268;
        }

        .btn:disabled {
            background: #ccc;
            cursor: not-allowed;
        }

        .alert {
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1rem;
            display: none;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .alert-danger {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        .loading {
            display: none;
            text-align: center;
            margin: 1rem 0;
        }

        .spinner {
            border: 4px solid #f3f3f3;
            border-top: 4px solid #667eea;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            animation: spin 2s linear infinite;
            margin: 0 auto 1rem;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .footer {
            text-align: center;
            margin-top: 2rem;
            padding: 1rem;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Payment</h1>
            <p class="subtitle">Complete your activity registration</p>
        </header>

        <div id="alert-container"></div>

        <div class="payment-container">
            <div class="payment-details">
                <h2>Payment Details</h2>
                <div class="payment-info">
                    <div>
                        <p><strong>Activity:</strong> <span id="payment-activity">Loading...</span></p>
                        <p><strong>Registration ID:</strong> <span id="payment-registration-id">Loading...</span></p>
                        <p><strong>Student ID:</strong> <span id="payment-student-id">Loading...</span></p>
                    </div>
                    <div>
                        <p><strong>Name:</strong> <span id="payment-name">Loading...</span></p>
                        <p><strong>Email:</strong> <span id="payment-email">Loading...</span></p>
                        <p><strong>Amount:</strong> $<span id="payment-amount">0</span></p>
                    </div>
                </div>
            </div>

            <div class="payment-methods">
                <h3>Select Payment Method</h3>
                
                <div class="payment-option" onclick="selectPaymentMethod('credit-card')">
                    <input type="radio" id="credit-card" name="payment-method" value="credit-card">
                    <div class="payment-icon">💳</div>
                    <div>
                        <strong>Credit Card</strong>
                        <p>Pay with Visa, MasterCard, or American Express</p>
                    </div>
                </div>
                
                <div class="payment-option" onclick="selectPaymentMethod('paypal')">
                    <input type="radio" id="paypal" name="payment-method" value="paypal">
                    <div class="payment-icon">🔵</div>
                    <div>
                        <strong>PayPal</strong>
                        <p>Pay with your PayPal account</p>
                    </div>
                </div>
                
                <div class="payment-option" onclick="selectPaymentMethod('bank-transfer')">
                    <input type="radio" id="bank-transfer" name="payment-method" value="bank-transfer">
                    <div class="payment-icon">🏦</div>
                    <div>
                        <strong>Bank Transfer</strong>
                        <p>Transfer directly from your bank account</p>
                    </div>
                </div>
            </div>

            <div class="loading" id="payment-loading">
                <div class="spinner"></div>
                <p>Processing your payment...</p>
            </div>

            <div style="text-align: center;">
                <button class="btn btn-success" id="pay-button" onclick="processPayment()" disabled>Complete Payment</button>
                <a href="/" class="btn btn-secondary">Cancel</a>
            </div>
        </div>

        <div class="footer">
            <p>Activity Registration System &copy; 2025</p>
        </div>
    </div>

    <script>
        // Global variables
        let registrationData = null;
        let selectedPaymentMethod = null;

        // DOM elements
        const alertContainer = document.getElementById('alert-container');
        const payButton = document.getElementById('pay-button');
        const paymentLoading = document.getElementById('payment-loading');

        // Initialize payment page
        document.addEventListener('DOMContentLoaded', function() {
            loadRegistrationData();
        });

        // Load registration data from sessionStorage
        function loadRegistrationData() {
            const storedData = sessionStorage.getItem('pendingPaymentRegistration');
            
            if (!storedData) {
                showAlert('No payment information found. Please start from the registration page.', 'danger');
                return;
            }
            
            try {
                registrationData = JSON.parse(storedData);
                displayPaymentDetails();
            } catch (error) {
                showAlert('Error loading payment information.', 'danger');
            }
        }

        // Display payment details on the page
        function displayPaymentDetails() {
            if (!registrationData) return;
            
            // Find activity name
            const activityName = getActivityName(registrationData.activityId);
            
            // Update payment details
            document.getElementById('payment-activity').textContent = activityName;
            document.getElementById('payment-registration-id').textContent = registrationData.id;
            document.getElementById('payment-student-id').textContent = registrationData.studentId;
            document.getElementById('payment-name').textContent = registrationData.name;
            document.getElementById('payment-email').textContent = registrationData.email;
            document.getElementById('payment-amount').textContent = registrationData.paymentAmount || 0;
        }

        // Get activity name by ID (in a real app, this would come from an API)
        function getActivityName(activityId) {
            const activities = {
                1: 'Tech Conference 2025',
                2: 'Coding Workshop',
                3: 'AI Seminar',
                4: 'Robotics Competition',
                5: 'Career Fair 2025',
                6: 'Hackathon 2025',
                7: 'Science Exhibition',
                8: 'Business Pitch Competition'
            };
            
            return activities[activityId] || 'Unknown Activity';
        }

        // Select payment method
        function selectPaymentMethod(method) {
            selectedPaymentMethod = method;
            
            // Update UI
            document.querySelectorAll('.payment-option').forEach(option => {
                option.classList.remove('selected');
            });
            
            document.getElementById(method).parentElement.classList.add('selected');
            
            // Enable pay button
            payButton.disabled = false;
        }

        // Process payment
        async function processPayment() {
            if (!selectedPaymentMethod) {
                showAlert('Please select a payment method', 'danger');
                return;
            }
            
            if (!registrationData) {
                showAlert('Payment information not found', 'danger');
                return;
            }
            
            // Show loading
            paymentLoading.style.display = 'block';
            payButton.disabled = true;
            
            try {
                const response = await fetch('/api/payments/process', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        registrationId: registrationData.id,
                        paymentMethod: selectedPaymentMethod
                    })
                });
                
                const result = await response.json();
                
                if (result.success) {
                    showAlert('Payment completed successfully!', 'success');
                    
                    // Clear session storage
                    sessionStorage.removeItem('pendingPaymentRegistration');
                    
                    // Redirect to home page after a delay
                    setTimeout(() => {
                        window.location.href = '/';
                    }, 2000);
                } else {
                    showAlert('Payment failed: ' + result.message, 'danger');
                    payButton.disabled = false;
                }
            } catch (error) {
                showAlert('Error processing payment: ' + error.message, 'danger');
                payButton.disabled = false;
            } finally {
                paymentLoading.style.display = 'none';
            }
        }

        // Show alert message
        function showAlert(message, type) {
            const alert = document.createElement('div');
            alert.className = `alert alert-${type}`;
            alert.textContent = message;
            alert.style.display = 'block';
            
            alertContainer.appendChild(alert);
            
            setTimeout(() => {
                alert.remove();
            }, 5000);
        }
    </script>
</body>
</html>
